
**Ideas**
- Since we don't have the data to make informed decisions about our users like Google (because we have no users), we can use other metrics to approximate the value of websites (text file of traffic, various other metrics that might indicate page value).
